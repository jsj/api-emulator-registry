export {
  cloudflarePlugin,
  cloudflarePlugin as default,
  plugin,
  contract,
  createCloudflareBindings,
  createCloudflarePlatform,
  label,
  endpoints,
  manifest,
  capabilities,
} from "./routes/http";
